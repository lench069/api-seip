{
  "name": "node-fastify-ssn-api",
  "description": "Microservice to interact with the SSN API using Fastify",
  "version": "1.0.0",
  "private": true,
  "main": "dist/server/index.js",
  "scripts": {
    "dev": "nodemon --watch 'src/**' --ext 'ts,json' --exec 'npm run build && npm start'",
    "build": "tsc",
    "start": "node dist/server/index.js"
  },
  "author": "Gemini",
  "license": "Apache-2.0",
  "dependencies": {
    "fastify": "^4.27.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "typescript": "^5.4.5",
    "@types/node": "^20.12.12"
  }
}